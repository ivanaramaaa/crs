$('#update_total').html("<%= escape_javascript (render partial: 'check_coupon_code') %>");

var discount = <%= @discount %>; 
  
$.ajax({
  url: '/registration_summary',
  type: 'POST',
  data: { discount: discount }
});